import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
    palette: {
      primary: {
        main: "#039091"
      }
    },
    typography: {
      fontSize: 14.5,
      fontFamily: "var(--font-family)"
    }
});